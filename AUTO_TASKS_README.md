# 自动任务系统使用指南

## 概述

本系统实现了全自动化的股票投资顾问智能体，包含以下五个核心模块的自动执行：

1. **策略筛选**：自动执行5日趋势核心、5日容量核心、龙头精选三种策略
2. **大牛股复盘**：自动进行大牛股潜力分析和特征提取
3. **跟踪观察**：自动执行跟踪初始化、T+1观察、T+3观察、跟踪验证
4. **经验生成**：自动生成成功经验和失败复盘
5. **任务调度**：统一的任务调度系统管理所有自动任务

## 自动任务列表

### 策略筛选任务（5个）

| 任务名称 | 执行时间 | 说明 |
|---------|---------|------|
| 早盘策略筛选 | 9:35 | 开盘后35分钟执行所有策略筛选 |
| 早盘二次筛选 | 10:35 | 上午10点35分再次执行策略筛选 |
| 早盘三次筛选 | 11:35 | 上午11点35分第三次执行策略筛选 |
| 午盘策略筛选 | 13:35 | 下午开盘后35分钟执行策略筛选 |
| 尾盘策略筛选 | 14:35 | 尾盘前执行策略筛选 |

### 大牛股复盘任务（2个）

| 任务名称 | 执行时间 | 说明 |
|---------|---------|------|
| 早盘大牛股复盘 | 10:00 | 上午10点执行大牛股复盘分析 |
| 午盘大牛股复盘 | 14:00 | 下午2点执行大牛股复盘分析 |

### 跟踪观察任务（4个）

| 任务名称 | 执行时间 | 说明 |
|---------|---------|------|
| 跟踪初始化 | 9:30 | 开盘时初始化新的跟踪任务 |
| T+1观察 | 9:35 | 获取跟踪股票次日数据 |
| T+3观察 | 9:35 | 获取跟踪股票第三日数据并评估 |
| 跟踪验证 | 15:30 | 收盘后验证并完成跟踪记录 |

### 经验生成任务（1个）

| 任务名称 | 执行时间 | 说明 |
|---------|---------|------|
| 经验生成 | 15:35 | 收盘后批量生成经验和复盘 |

## 使用方法

### 1. 初始化系统

首次使用需要初始化自动任务系统：

```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"action":"init"}' \
  http://localhost:5000/api/auto-tasks
```

### 2. 启动所有任务

启动后，所有任务将按照预设的定时规则自动执行：

```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"action":"start"}' \
  http://localhost:5000/api/auto-tasks
```

### 3. 停止所有任务

停止所有自动任务的执行：

```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"action":"stop"}' \
  http://localhost:5000/api/auto-tasks
```

### 4. 手动触发任务

可以手动触发任何任务立即执行：

```bash
curl -X POST -H "Content-Type: application/json" \
  -d '{"action":"trigger","taskType":"STRATEGY_MORNING_1"}' \
  http://localhost:5000/api/auto-tasks
```

可用任务类型：
- `STRATEGY_MORNING_1` - 早盘策略筛选
- `STRATEGY_MORNING_2` - 早盘二次筛选
- `STRATEGY_MORNING_3` - 早盘三次筛选
- `STRATEGY_AFTERNOON_1` - 午盘策略筛选
- `STRATEGY_AFTERNOON_2` - 尾盘策略筛选
- `BULL_ANALYSIS_MORNING` - 早盘大牛股复盘
- `BULL_ANALYSIS_AFTERNOON` - 午盘大牛股复盘
- `TRACKING_INIT` - 跟踪初始化
- `T1_OBSERVATION` - T+1观察
- `T3_OBSERVATION` - T+3观察
- `TRACKING_VERIFY` - 跟踪验证
- `EXPERIENCE_GENERATE` - 经验生成

### 5. 查看任务状态

查看所有任务的运行状态：

```bash
curl 'http://localhost:5000/api/auto-tasks?action=status'
```

## Web界面操作

1. 打开主页，点击"🤖 自动任务"标签页
2. 点击"初始化系统"按钮初始化任务系统
3. 点击"启动所有任务"按钮开始自动执行
4. 可以查看每个任务的最后运行时间、执行状态和错误信息
5. 点击"立即执行"按钮可以手动触发任何任务

## 执行时间表

```
09:30 - 跟踪初始化
09:35 - 早盘策略筛选、T+1观察、T+3观察
10:00 - 早盘大牛股复盘
10:35 - 早盘二次筛选
11:35 - 早盘三次筛选
13:35 - 午盘策略筛选
14:00 - 午盘大牛股复盘
14:35 - 尾盘策略筛选
15:30 - 跟踪验证
15:35 - 经验生成
```

## 注意事项

1. 所有任务仅在周一至周五（工作日）执行
2. 系统使用中国标准时间（Asia/Shanghai）
3. 确保数据库连接正常，否则任务会失败
4. 建议在开盘前（9:25）启动系统
5. 可以通过日志文件查看任务执行详情

## 日志查看

```bash
# 查看实时日志
tail -f /app/work/logs/bypass/app.log

# 查看最近100行日志
tail -n 100 /app/work/logs/bypass/app.log

# 搜索错误日志
grep -i error /app/work/logs/bypass/app.log
```

## 故障排除

### 任务未按计划执行

1. 检查系统时间是否正确
2. 确认任务已启动（查看任务状态）
3. 检查日志文件查看错误信息

### 数据库连接失败

1. 检查环境变量是否正确配置
2. 确认Supabase服务可用
3. 检查网络连接

### 任务执行失败

1. 查看日志文件定位错误原因
2. 尝试手动触发任务测试
3. 检查依赖服务（数据源、数据库）是否正常

## 技术架构

- **调度器**：node-cron
- **数据源**：东方财富
- **数据库**：Supabase
- **后端框架**：Next.js 16
- **前端框架**：React 19
